# Morphius/morpheus-pod/requirements_pod.txt

# --- Dependencias Principales de Morpheus Pod ---
fastapi
uvicorn[standard]
python-multipart
gunicorn
Flask
python-dotenv
transformers
protobuf

# --- [MOTOR UPGRADE] Dependencias para los nuevos modelos ---
# SD3 y SVD requieren versiones actualizadas de estas librerías
diffusers>=0.27.0
accelerate>=0.28.0

# --- [NUEVO] Módulo 1: Dependencias de Audio Avanzado ---
TTS # CoquiTTS para Texto-a-Voz de alta calidad
audiocraft # Para generación de SFX y música (AudioGen)
demucs # Para separación de fuentes y limpieza de audio

# --- [NUEVO] Módulo 2: Dependencias de Edición de Vídeo ---
ProPainter # Para inpainting/outpainting en vídeo

# --- [NUEVO] Módulo 3: Dependencias de Transcripción de Audio ---
openai-whisper

# --- [NUEVO] Módulo 4: Dependencias de Hardware y Benchmark ---
psutil
pynvml

# --- [SECCIÓN DE FIJACIÓN DE VERSIONES (PINNING) PARA ESTABILIDAD] ---
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
numpy==2.2.4

# --- Dependencias que faltaban y que causaban ModuleNotFoundError ---
torchsde
opencv-python-headless
librosa

# --- Paquetes esenciales de ComfyUI y nodos que ahora se instalan vía pip ---
comfyui-workflow-templates
comfyui-embedded-docs
imageio-ffmpeg
# Para entrenamiento de LoRA (esto instala la suite de Kohya)
kohya_ss
pillow
# Para segmentación facial avanzada, detección de landmarks y animación (requiere OpenCV)
face-alignment
# Para el streaming de vídeo en "Modo Espelo" (Actuación Virtual)
streamlit-webrtc
av
