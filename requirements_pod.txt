# Dependencias principales de Morpheus Pod
fastapi
uvicorn[standard]
python-multipart
gunicorn
Flask
python-dotenv
transformers

# --- [SECCIÓN DE FIJACIÓN DE VERSIONES (PINNING) PARA ESTABILIDAD] ---

# Fija el "trío" compatible para resolver el conflicto torch/torchvision/torchaudio.
# Estas versiones están garantizadas para funcionar juntas.
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# Fija numpy a una versión compatible con opencv-python Y torch.
# El error demostró que opencv-python requiere una versión <2.3.0.
numpy==2.2.4

# Dependencias que faltaban y que causaban ModuleNotFoundError, ahora explícitas.
torchsde
opencv-python-headless
