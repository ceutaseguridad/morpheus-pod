# Dependencias principales de Morpheus Pod
fastapi
uvicorn[standard]
python-multipart
gunicorn
Flask
python-dotenv
transformers
protobuf
accelerate

# --- [SECCIÓN DE FIJACIÓN DE VERSIONES (PINNING) PARA ESTABILIDAD] ---

# Fija el "trío" compatible para resolver el conflicto torch/torchvision/torchaudio.
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# Fija numpy a una versión compatible con opencv-python Y torch.
numpy==2.2.4

# Dependencias que faltaban y que causaban ModuleNotFoundError.
torchsde
opencv-python-headless
librosa

# Paquetes esenciales de ComfyUI y nodos que ahora se instalan vía pip.
comfyui-workflow-templates
comfyui-embedded-docs
imageio-ffmpeg

# Para entrenamiento de LoRA (esto instala la suite de Kohya)
kohya_ss
pillow

# --- [VERITAS UPGRADE] Nuevas dependencias ---

# Para segmentación facial avanzada, detección de landmarks y animación (requiere OpenCV)
face-alignment

# Para el streaming de vídeo en "Modo Espejo" (Actuación Virtual)
streamlit-webrtc
av
