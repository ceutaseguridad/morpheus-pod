# Morpheus Pod - Requisitos del Servidor v2.2
# Estas son las dependencias EXCLUSIVAS de los servidores de Morpheus (worker y file server).
# NO incluir 'torch' o 'torchvision' aquí, ya que confiamos en la versión de la imagen base de RunPod.

# Servidores de API
fastapi>=0.103.1,<0.111.0 # Fijamos un rango compatible
uvicorn[standard]>=0.23.2,<0.28.0
gunicorn
Flask>=3.0.0,<3.1.0

# Modelos de datos y comunicación
pydantic>=2.5.0,<2.7.0 # Pydantic v2 es requerido por FastAPI reciente

# Dependencias del LLM y la lógica de IA del worker_server
# Es crucial que estas versiones sean compatibles con el PyTorch 2.4 de la imagen.
transformers>=4.38.0,<4.41.0
accelerate>=0.28.0 # A menudo requerido por transformers para optimizaciones
sentencepiece # Requerido por muchos tokenizers de transformers

# Utilidades de red y archivos
python-multipart==0.0.6
piexif==1.1.3
requests
websocket-client
